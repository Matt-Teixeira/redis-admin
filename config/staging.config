# Basic persistence & safe defaults
appendonly yes
save 900 1
save 300 10
save 60 10000


# You can add instance-specific tweaks here if needed.

# Auth (REDIS-01 rollout 2026-08-18): this instance DELIBERATELY has no
# `include auth.conf` line — its consumer is odd-jobs, whose Redis client has
# no password support. It is still a required part of the standard build
# (compose brings up all four instances together). Revisit auth here only
# with Jonathan; the other three configs show the include pattern to copy.
